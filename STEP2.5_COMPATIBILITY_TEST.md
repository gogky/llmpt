# Step 2.5: 协议兼容性验证 - 详细操作步骤

## 🎯 验证目标

验证我们实现的 Tracker Server 能否与标准 BitTorrent 客户端（qBittorrent、Transmission）正常工作。

**成功标准**：
- ✅ qBittorrent 和 Transmission 能通过 Tracker 互相发现
- ✅ 文件能成功传输
- ✅ Tracker 正确记录 Peers 和统计信息

---

## 📋 准备工作

### 1. 环境要求

- **两台设备**（或一台电脑 + 一台虚拟机）
  - 设备 A：运行 Tracker Server + qBittorrent（做种）
  - 设备 B：运行 Transmission（下载）
  
- **网络要求**：
  - 两台设备在同一局域网内
  - 能互相 ping 通
  - 防火墙允许 BT 端口通信（默认 6881-6889）

### 2. 下载软件

#### 设备 A（Windows）

**qBittorrent v4.6+**
```
下载地址: https://www.qbittorrent.org/download
选择: Windows x64 安装版
```

**或者使用 Chocolatey**：
```powershell
choco install qbittorrent
```

#### 设备 B（Windows/Linux/Mac）

**Transmission**

- **Windows**: https://transmissionbt.com/download/
- **Linux**: `sudo apt install transmission-gtk`
- **Mac**: `brew install --cask transmission`

---

## 🚀 详细操作步骤

### Phase 1: 启动 Tracker Server

#### Step 1.1 - 启动数据库

在**设备 A**上执行：

```powershell
# 进入项目目录
cd d:\workspace\llmpt

# 启动数据库
docker-compose up -d

# 验证数据库运行
docker-compose ps
```

预期输出：
```
NAME                STATUS
llmpt-mongodb-1     Up
llmpt-redis-1       Up
```

#### Step 1.2 - 获取本机 IP

```powershell
# 获取本机 IP（记录下来，后面会用）
ipconfig | findstr IPv4
```

假设你的 IP 是：`192.168.1.100`

#### Step 1.3 - 启动 Tracker

```powershell
cd cmd\tracker
go run main.go
```

预期输出：
```
🚀 Starting Tracker Server...
✅ Database connected
🎯 Tracker Server listening on :8080
📡 Announce endpoint: http://localhost:8080/announce
```

**保持这个窗口运行，不要关闭！**

---

### Phase 2: 准备测试文件

#### Step 2.1 - 创建测试文件

在**设备 A**上创建一个测试文件（用于做种）：

```powershell
# 创建测试目录
mkdir C:\BTTest
cd C:\BTTest

# 创建一个 100MB 的测试文件
fsutil file createnew test_file_100MB.bin 104857600

# 或者创建一个包含多个文件的文件夹
mkdir TestFolder
cd TestFolder
fsutil file createnew file1.txt 10485760
fsutil file createnew file2.txt 10485760
fsutil file createnew file3.txt 10485760
```

> 💡 **建议**: 文件大小 50-200MB 即可，太小看不出效果，太大测试时间长

---

### Phase 3: 用 qBittorrent 制作种子并做种

#### Step 3.1 - 打开 qBittorrent

1. 启动 qBittorrent
2. 首次运行会有设置向导，点击"下一步"完成

#### Step 3.2 - 制作种子

1. 点击菜单：**工具 (Tools) → Torrent Creator**

2. 填写种子创建信息：

   | 字段 | 值 |
   |------|-----|
   | **Select file/folder** | 选择 `C:\BTTest\test_file_100MB.bin` |
   | **Tracker URLs** | `http://192.168.1.100:8080/announce` |
   | **Piece size** | 选择 **4 MiB** 或 **8 MiB**（大文件优化） |
   | **Private torrent** | ✅ **勾选此项**（强制使用 Tracker） |
   | **Start seeding** | ✅ **勾选此项**（创建后自动开始做种） |

3. 点击 **Create Torrent**

4. 保存种子文件：
   - 文件名：`test_file.torrent`
   - 保存到：`C:\BTTest\test_file.torrent`

#### Step 3.3 - 验证做种状态

1. 种子创建完成后，qBittorrent 会自动添加并开始做种

2. 在 qBittorrent 主界面查看：
   - **状态**: 应显示 "做种中 (Seeding)" 或 "Up"
   - **连接数**: 暂时为 0（因为还没有下载者）
   - **Tracker 状态**: 点击种子，查看底部 "Tracker" 标签

3. 检查 Tracker 状态：
   - 应显示 "正在工作 (Working)"
   - **Peers**: 0 个

#### Step 3.4 - 验证 Tracker 收到请求

回到 Tracker Server 窗口，应该能看到日志：

```
2026/02/05 16:30:15 GET /announce from 192.168.1.100:54321
2026/02/05 16:30:15 Request completed in 5.234ms
```

#### Step 3.5 - 检查 Redis 数据

打开新的 PowerShell 窗口：

```powershell
docker exec -it llmpt-redis-1 redis-cli

# 查看所有 Tracker 相关的 Key
KEYS tracker:*

# 应该能看到类似：
# 1) "tracker:peers:abc123..."
# 2) "tracker:stats:abc123..."

# 查看 Peer 列表（复制实际的 info_hash）
SMEMBERS tracker:peers:abc123...

# 应该能看到：
# 1) "192.168.1.100:6881"

# 查看统计信息
HGETALL tracker:stats:abc123...

# 应该能看到：
# 1) "seeders"
# 2) "1"
# 3) "leechers"
# 4) "0"
```

✅ **如果看到以上信息，说明 Tracker 正常工作！**

---

### Phase 4: 传输种子文件到设备 B

#### 方法 1：通过网络共享（推荐）

**设备 A**：
```powershell
# 共享 BTTest 文件夹
# 右键点击 C:\BTTest → 属性 → 共享 → 高级共享
# 勾选"共享此文件夹" → 权限 → 允许"读取"
```

**设备 B**：
```powershell
# 访问共享文件夹
\\192.168.1.100\BTTest
# 复制 test_file.torrent 到本地
```

#### 方法 2：通过 U 盘或其他方式

直接复制 `test_file.torrent` 到设备 B

---

### Phase 5: 用 Transmission 下载

#### Step 5.1 - 启动 Transmission

在**设备 B**上启动 Transmission

#### Step 5.2 - 添加种子

1. 点击 **File → Open Torrent** 或拖拽 `test_file.torrent`

2. 选择下载位置（不要选择与做种文件相同的位置）

3. 点击 **OK** 开始下载

#### Step 5.3 - 观察连接过程

**在 Transmission 中**：
- **状态**: "正在下载 (Downloading)"
- **Peers**: 应该显示连接到 1 个 Peer
- **来源**: `192.168.1.100:6881`

**在 qBittorrent 中**：
- **连接数**: 应该变成 1
- **上传速度**: 开始显示上传速度

**在 Tracker 日志中**：
```
2026/02/05 16:35:20 GET /announce from 192.168.1.50:54322  (设备 B 的 IP)
2026/02/05 16:35:20 Request completed in 4.123ms
```

#### Step 5.4 - 验证传输

等待下载完成（根据文件大小和网络速度）

**预期结果**：
- Transmission 显示 "已完成 (Finished)"
- qBittorrent 显示有 1 个连接的 Peer
- 文件哈希校验通过（自动进行）

---

### Phase 6: 验证结果

#### Step 6.1 - 检查 Redis 数据

```powershell
docker exec -it llmpt-redis-1 redis-cli

# 查看 Peer 列表
SMEMBERS tracker:peers:abc123...

# 应该看到两个 Peer：
# 1) "192.168.1.100:6881"  (qBittorrent)
# 2) "192.168.1.50:51413"   (Transmission)

# 查看统计信息
HGETALL tracker:stats:abc123...

# 应该看到：
# 1) "seeders"
# 2) "2"        (两个都完成了)
# 3) "completed"
# 4) "1"        (完成次数)
```

#### Step 6.2 - 验证文件完整性

在**设备 B**上：

```powershell
# 计算文件哈希（验证文件完整性）
certutil -hashfile "下载的文件路径" SHA256

# 与原文件对比
# 在设备 A 上运行：
certutil -hashfile C:\BTTest\test_file_100MB.bin SHA256

# 两个哈希值应该完全一致
```

---

## ✅ 成功标准检查清单

- [ ] Tracker Server 成功启动，监听在 8080 端口
- [ ] qBittorrent 成功制作种子，Tracker 显示"正在工作"
- [ ] Redis 中能看到 qBittorrent 的 Peer 记录
- [ ] Transmission 成功添加种子并开始下载
- [ ] 两个客户端能互相发现（在 Peer 列表中看到对方）
- [ ] 文件传输成功完成
- [ ] Redis 统计信息正确（seeders, completed 等）
- [ ] 文件哈希校验一致

如果以上 8 项全部通过，恭喜！**Step 2.5 验证成功！✅**

---

## 🐛 常见问题排查

### 问题 1: qBittorrent 中 Tracker 显示"未连接"

**可能原因**：
1. Tracker URL 填写错误
2. Tracker Server 未运行
3. 防火墙阻止

**解决方法**：
```powershell
# 1. 验证 Tracker 是否运行
curl http://192.168.1.100:8080/health

# 2. 检查防火墙（临时关闭测试）
# Windows: 控制面板 → Windows Defender 防火墙 → 关闭

# 3. 在 qBittorrent 中手动更新 Tracker
# 右键种子 → 编辑 Tracker → 强制重新通知
```

### 问题 2: Transmission 找不到 Peer

**可能原因**：
1. 两台设备不在同一网络
2. 端口被阻止
3. 种子未标记为"私有"

**解决方法**：
```powershell
# 1. 验证网络连通性
# 在设备 B 上 ping 设备 A
ping 192.168.1.100

# 在设备 B 上测试 Tracker 端口
Test-NetConnection -ComputerName 192.168.1.100 -Port 8080

# 2. 检查 qBittorrent 监听端口
# qBittorrent → 设置 → 连接 → 监听端口（默认 6881）

# 3. 确认种子是私有的
# 重新制作种子时务必勾选 "Private torrent"
```

### 问题 3: 连接上了但传输速度为 0

**可能原因**：
1. 两个客户端都在 NAT 后面
2. 端口映射问题

**解决方法**：
```powershell
# 1. 在两个客户端中手动指定端口
# qBittorrent: 设置 → 连接 → 监听端口 → 6881
# Transmission: 编辑 → 首选项 → 网络 → 监听端口 → 51413

# 2. 禁用 UPnP 和 NAT-PMP（局域网测试不需要）
# qBittorrent: 设置 → 连接 → 取消勾选 UPnP/NAT-PMP

# 3. 检查是否使用了代理
# 确保两个客户端都没有启用代理
```

### 问题 4: Redis 中看不到 Peer

**可能原因**：
1. info_hash 编码问题
2. Tracker 解析请求失败

**解决方法**：
```powershell
# 查看 Tracker 日志，寻找错误信息
# 在 Tracker Server 窗口中应该能看到请求日志

# 如果有错误，检查：
# 1. URL 编码是否正确
# 2. 参数是否完整
```

---

## 📊 高级测试（可选）

### 测试 1: 多个下载者

1. 在第三台设备上也安装 Transmission
2. 添加相同的种子
3. 验证三个客户端能互相发现

### 测试 2: 断点续传

1. Transmission 下载到 50% 时，关闭程序
2. 重新打开 Transmission
3. 验证能从 50% 继续下载

### 测试 3: 心跳超时

1. 在 qBittorrent 中停止做种
2. 等待 30 分钟（TTL 过期）
3. 检查 Redis，Peer 应该自动被清除

```powershell
# 检查 TTL
docker exec -it llmpt-redis-1 redis-cli
TTL tracker:peers:abc123...

# 应该显示剩余秒数，30分钟后变成 -2（已过期）
```

---

## 📝 测试报告模板

完成测试后，可以填写以下报告：

```markdown
## Step 2.5 验证报告

**测试日期**: 2026-02-XX
**测试人员**: XXX

### 环境信息
- 设备 A: Windows 11, qBittorrent 4.6.0
- 设备 B: Windows 10, Transmission 4.0.5
- 网络: 局域网 (192.168.1.x)
- 文件大小: 100 MB

### 测试结果
- [x] Tracker Server 启动成功
- [x] 种子制作成功
- [x] qBittorrent 连接 Tracker 成功
- [x] Transmission 发现 Peer 成功
- [x] 文件传输成功
- [x] 哈希校验通过
- [x] Redis 数据正确

### 性能数据
- 传输速度: XX MB/s
- 完成时间: XX 秒
- Tracker 响应时间: ~5ms

### 问题记录
（无问题 / 记录遇到的问题和解决方法）

### 结论
✅ Step 2.5 验证通过，Tracker Server 完全兼容标准 BitTorrent 协议
```

---

## 🎉 验证完成后

如果验证成功，你可以：

1. ✅ 标记 Step 2.5 为完成
2. 🚀 开始 Step 3: CLI 客户端开发
3. 📸 截图保存测试结果（可选）
4. 🎯 准备开发 `model-cli` 工具

---

**项目**: llmpt - 大模型 P2P 分享站  
**阶段**: Step 2.5 - 协议兼容性验证  
**更新时间**: 2026-02-05
